已完成对网站与政策文档的交叉审查。下面是针对"低价值内容"被拒的**根因诊断 + 具体优化建议**。

---

## 一、结论：被拒的核心原因

审核结论"低价值内容"（Low-value content）在 AdSense 政策里对应的是 **"内容贫乏/规模化内容"**（`垃圾内容政策.md` 的"滥用规模化内容" + `优质内容政策.md` 的"质量低劣或内容肤浅的网页"）。

我审查了 `src/data/posts/` 下约 22 个主题的 45 篇中英文文章，发现**网站内容本身可读性不差**（装修指南、健康饮食、Python 等写得挺深入），但存在三个足以触发"低价值内容"判定的结构性问题：

---

## 二、核心问题诊断

### 问题 1（最致命）：博客主题严重发散，与网站定位脱节

你的网站是"青枣工作室"，产品是**装修导图工具**和**AI 抠图工具**。但博客却覆盖了完全无关的主题：

| 与定位相关（应保留） | 完全离题（应删除/高风险） |
|---|---|
| 装修全流程、装修预算、装修避坑、装修风格、小户型装修、全屋智能、家居收纳 | 电动汽车趋势 `electric-vehicle-trends` |
| 批量抠图、在线抠图工具、图片格式、产品摄影 | 健康饮食 `healthy-diet-habits` |
| 工具选型（可保留并改写） | 正念冥想 `mindfulness-meditation` |
| | Python 数据分析、React Hooks、Web 设计基础 |
| | 初创公司营销、时间管理、SEO 优化 |

约 **9/22 ≈ 40%** 的文章与网站业务无关。这正是 `垃圾内容政策.md` 描述的"创建许多网页，主要目的是操纵搜索排名而不是帮助用户"+"内容对读者几乎没有意义"的**规模化内容（scaled content）**特征。一个什么话题都写的站点，Google 无法判断它的核心主题与专业度（E-E-A-T），会直接判为低价值。

### 问题 2：规模化/AI 生成痕迹明显

- 多个 `generate-*-posts.mjs` 脚本 + `fix_json_format.py`、`verify-posts.mjs` 等，表明文章是**批量程序化生成**的（`simple-generate.mjs` 里把整篇文章写死在模板字符串里）。
- 发布时间高度集中：文章日期全在 `2026-06-05 ~ 06-14`（10 天约 22 篇，≈2 篇/天），对一个新站是异常爆发。
- 大量文章采用同一套公式化结构："你 90% 都错了""只需 5 件事"、通篇 emoji、标题党化。这是内容农场的典型签名。

### 问题 3：部分文章偏浅

`posts-zh.json` 里如 `seo-optimization-guide`、`web-design-basics`、`react-hooks-introduction` 的 excerpt 是模板化描述（"本文介绍 X 的基本原理和实用技巧"），正文大概率偏薄，属于政策里"内容贫乏的网页"。

### 问题 4（技术风险）：SPA 正文靠 JS 运行时渲染

博客正文在 `BlogPostPage.jsx` 里通过 `getPostContent()` 从 JSON + .md **运行时加载**，初始 HTML 不含正文。AdSense 审核爬虫虽能渲染 JS，但 SPA 内容可抓取性始终弱于静态页，正文若加载失败/延迟就容易被判"内容稀少"。

---

## 三、已合规、无需改动的项（给你信心）

- ✅ 隐私政策已披露 Google AdSense 与 DoubleClick Cookie（`zh-CN.json` 第五节）
- ✅ `robots.txt` 允许抓取、`sitemap.xml` 已包含博客文章
- ✅ 无"建设中/敬请期待"占位页（已全文搜索确认）
- ✅ 有 About / Contact / Products / Disclaimer / 404 页面，导航完整
- ✅ 每篇文章有 JSON-LD 结构化数据 + canonical

---

## 四、具体优化建议（按优先级）

### P0 — 必须做（直接消除"规模化内容"信号）
1. **删除全部离题文章**（约 9 篇：电动车、健康饮食、正念、Python、React、Web 设计、初创营销、时间管理、SEO）。保留并强化 **装修 + 图片处理** 两条主线，使博客与产品强相关。
2. **停止爆发式发布**，改为稳定节奏（每周 1–2 篇），让站点看起来是"被持续维护的真实站点"而非内容农场。
3. 删除冗余的生成脚本（`generate-*-posts.mjs` 一大堆同名变体），避免后续又被批量生成。

### P1 — 提升内容"价值"信号
4. **深化保留的文章**：每篇目标 ≥800 字、带原创经验/数据/案例，去掉"你 90% 都错了"这类标题党，改为回答真实搜索意图的标题（如"90㎡ 装修到底要花多少钱"）。
5. 增加**作者/工作室署名**与"编辑方针"说明页，证明是真实、有主体的站点。
6. 在文章间做**内链**（装修类互链、抠图类互链），形成主题簇，强化 topical authority。

### P2 — 技术可抓取性
7. **预渲染博客正文为静态 HTML**（Vite + `vite-plugin-ssr` / 构建时生成，或把博客改静态页），确保 AdSense 爬虫拿到的首屏 HTML 就含全文。
8. 提交 sitemap 到 Google Search Console，确认博客文章已被索引、无重复 URL。

### P3 — 收尾
9. 审核前用 Search Console 自查：无 404、无重复 meta、无隐藏文字。
10. 重新提交审核时在申请里说明：已**移除离题/低质内容**、聚焦核心主题、补充原创深度内容。

---

## 五、建议保留的文章清单（聚焦后约 13 篇）

装修线：`decoration-guide-beginners`、`decoration-budget-guide`、`decoration-flowchart-tutorial`(直接讲产品)、`decoration-mistakes`、`decoration-style-comparison`、`small-apartment-decoration`、`smart-home-system`、`home-storage-solutions`
图片线：`batch-background-removal`、`online-background-removal-tools`、`image-format-guide`、`product-photo-guide`
工具线：`choose-right-tools`(建议改写为围绕本工作室工具)

删除其余 9 篇离题文章后，站点主题将高度聚焦，最贴合 AdSense"提供独特且有价值、与站点目的一致的内容"的要求。

---

需要我**直接执行 P0 的清理**（删除离题文章 + 它们的中英文 .md、JSON 条目、生成脚本），或**把博客改造成静态预渲染**来提升可抓取性吗？告诉我范围我就动手。

---

## 六、执行进度（自动记录）

- **P0**：已完成。仓库当前 13 篇博客文章（装修线 8 + 图片线 4 + 工具线 1）与第五节「保留清单」完全一致，离题文章已移除。
- **P1**：见对应会话（深化内容、作者署名、内链等）。
- **P2 — 技术可抓取性**：
  - **第 7 项（预渲染博客正文为静态 HTML）= 已完成（2026-07-14）**。
    - 新增共享模块 `src/data/posts/markdownToHtml.js`（纯 JS，客户端组件与构建脚本共用，避免逻辑漂移；顺带补全了原组件缺失的引用块 `>` 渲染）。
    - 重构 `BlogPostPage.jsx` 改用该模块（移除内联 `renderContent`）。
    - 重写 `scripts/prerender.mjs`：构建后为每篇博客生成 `dist/blog/<slug>.html`（扁平 .html，避免目录式 `index.html` 触发 Vercel 路径重定向），首屏即含全文 + 正确 `<title>`/`description`/`canonical`/JSON-LD（Article + BreadcrumbList）。
    - `npm run build:full` 已验证：13 篇博客文章均生成静态全文页；Chrome DevTools 实测页面正常渲染、无报错。
    - 注意：中英文文章共用同一 slug，静态文件只代表默认语言（中文），英文由 SPA 客户端切换。
  - **第 8 项（提交 sitemap 到 GSC）**：`scripts/generate-sitemap.mjs` 已生成 `public/sitemap.xml`（20 个 URL，含全部博客文章，无重复 URL）。**提交到 Google Search Console 并确认索引为人工操作，需在 GSC 后台完成。**

### P2 补充：GSC「未编入索引」修复（2026-07-15）

GSC 报告 12 个网页未编入索引，三类原因：① 网页会自动重定向；② 未找到（404）；③ 重复网页，Google 选择的规范网页与用户指定的不同。

**根因**：预渲染产物是目录式 `dist/blog/<slug>/index.html`，只有「带斜杠」URL 才命中真实内容；而 sitemap 与 canonical 都用「无斜杠」URL（`/blog/<slug>`）。Vercel 对目录 `index.html` 会把无斜杠 URL 308 重定向到带斜杠版本 → 被判为「自动重定向」；重定向后页面 canonical 仍是无斜杠，与 Google 实际收录的带斜杠 URL 不一致 → 「重复网页/规范冲突」；部分无斜杠 URL 无对应静态文件 → 404。

**修复**：`scripts/prerender.mjs` 的 `writeRoute` 改为生成扁平 `.html`（`dist/blog/<slug>.html`、`dist/about.html` 等），使「无斜杠」URL 直接命中真实静态文件（200、无重定向），与现有 sitemap/canonical（均为无斜杠）完全一致。

**验证**：`npm run build:full` 生成 22 个扁平 `.html`；本地 `vite preview` 实测 `/blog/smart-home-system`、`/about`、`/blog`、`/privacy`、`/contact` 无斜杠 URL 均返回 200、无重定向，canonical=`https://qingzao.site/blog/smart-home-system`；Chrome DevTools 渲染正常、无 JS 报错。

**用户侧待办**：重新部署后，在 GSC 重新提交 sitemap，并对受影响 URL 用「网址检查」请求编入索引（GSC 重新处理需数日）。若仍有个别 404，多为历史旧 URL（如早期英文路径），请用网址检查核对实际 URL。
- **P3**：见对应会话。