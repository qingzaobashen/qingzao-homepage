# 2026-09-11

## 系列分类重构为三大核心系列
- `src/data/series/series-zh.json` / `series-en.json` 由 5 个系列合并为 3 个（中英 order 必须完全一致）：
  - `decoration-full-process` 装修全流程系列 / Full Decoration Process（39 篇，含原装修全流程 + 验收标准 + 主材选购，并补齐此前未入系列的装修指南文章）
  - `life-and-mind` 机制人生系列 / Life & Mind Series（5 篇：living-body-and-soul、why-chinese-homes…、home-storage-solutions、smart-home-system、quiet-fortune-life-hacks）
  - `tools-and-tech` 工具与技术系列 / Tools & Tech Series（6 篇：抠图/图片格式/白底图 + choose-right-tools + ai-coding-august-experience）
  - 全站 50 篇文章已 100% 覆盖，文章互不重复。
- 废弃旧 slug：`renovation-acceptance`、`main-materials-guide`、`smart-home`、`image-processing`；在 `SeriesDetailPage.jsx` 加了 `LEGACY_SERIES_SLUGS` 映射 + `<Navigate replace>` 兼容跳转（避免旧收录链接 404）。
- 首页展示同步更新（zh-CN.json / en-US.json）：`home.featuredSeriesTitle`(三大核心系列)、`featuredSeries`(3 条卡片：slug/title/count/description/tags/image)、`categories[].subs` 链接与 `categoriesDesc`。
- 系列 `category` 字段沿用四分类词汇（装修指南/机制生活/工具评测）；文章 `category` 未改动（BlogListPage 的 ?category= 筛选依赖它）。
- `clusters.js`（文章页「相关主题」内链簇）保持原样，粒度比系列更细，未随之拆分。
- 已重新生成 `public/sitemap.xml`（63 个 URL，仅含新 3 个系列页）并跑通 `npm run build:full`（128 个预渲染路由）。
