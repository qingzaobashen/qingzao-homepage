# 2026-07-17 工作日志

## AdSense 自动广告脚本全站覆盖
- 需求：`adsbygoogle.js?client=ca-pub-3359350154774191` 要出现在每个页面 `<head>`（含构建期生成的博客/静态页）。
- 改动：`index.html` 原已含；在 `scripts/prerender.mjs` 新增 `ADSENSE_SCRIPT` 常量 + `ensureAdSenseScript(html)`（幂等），在 `writeRoute` 写入前强制注入。
- 验证：112/113 页面各含 1 个脚本；唯一例外 `google<id>.html` 是 AdSense 站点验证文件，正确排除。注意 `npm run build:full` 链式命令任一步失败会残留旧 dist，验证时可单独 `node scripts/prerender.mjs`。

## 制作「google-ads-compliance-audit」技能（用户级 Skill）
- 位置：`C:\Users\yuchen\.codebuddy\skills\google-ads-compliance-audit\`（用户级，跨项目可用，可经 use_skill 触发）。
- 结构：
  - `SKILL.md`：六大审查维度（内容质量/导航UX/必备页面/隐私数据/广告位/技术可信度）+ 工作流 + 严重级别（🔴阻断/🟠严重/🟡警告/🔵建议）+ 输出要求。
  - `references/policy-checklist.md`：逐条检查清单，映射 发布商政策/优质内容政策/垃圾内容政策/AdSense支持文档，含判定方法与整改建议。
  - `references/report-template.md`：合规诊断报告模板（总体结论+分维度明细+优先级行动清单）。
  - `scripts/audit_static_site.mjs`：静态站点技术项自动体检（广告代码覆盖率、ads.txt、robots、sitemap、必备页面、空/薄内容页、占位页、重复 title/description、非 HTTPS canonical）。用法 `node audit_static_site.mjs <dir> [--json]`。
- 依据源文档：`docs/Ads/googleAds政策/`（publisher政策、优质内容政策、垃圾内容政策、创建优质内容建议、低价值内容改进计划0714）。
- 试跑本项目 dist 结果：脚本正常；发现真实信号——21 个静态路由 `#root` 为空（仅博客文章注入了正文，静态页靠 SPA 客户端渲染，存在可抓取性隐患）；`ads.txt` 不在 dist（仓库根有 ads.txt 但 Vite 只拷贝 public/，需确认线上是否可访问）。

## 修复审计发现的问题（用户要求「修复你提到的这些问题」）
- **P1 静态页空 `#root`**：在 `scripts/prerender.mjs` 新增 `buildStaticBodyHtml(route, locale)`，从 `zhLocale`/`enLocale`（src/locales）与 `getPostsData()` 取数，生成首屏 HTML 注入 `#root`；`main()` 静态路由循环对中/英 `writeRoute` 传入 `bodyHtml`。覆盖 /、/products、/about、/contact、/blog、/privacy、/terms、/disclaimer、/editorial、/404 及 /en 版。React 水合后由客户端接管（内容与 locales 同源，无漂移）。
- **日期聚集（用户明确要求）**：17 篇文章原全为 2026-07-16。按用户说明（7.16 只是搬移日期，非真实写作日），将 16 篇改为 2026-05-17 → 2026-06-28 的两月跨度，保留 `bed-mattress-selection` 为 2026-07-16（最新）。`posts-zh.json` 与 `posts-en.json` 同步修改。
- **重建验证**：`npm run build:full`（generate-sitemap → vite build → prerender）成功。复验：`dist/ads.txt` 已随 public/ 拷贝生成；静态页 `#root` 均含真实内容；博客索引文章按日期倒序跨越 2026-05-05~2026-07-16。广告脚本覆盖 112/113，唯一缺失是 AdSense 站点验证文件（按设计不注入）。
- **审计两处「误报」需知悉**：① ads.txt「与页面一致❌」—— 页内 `ca-pub-3359350154774191` 与 ads.txt `pub-3359350154774191` 同发布商（`ca-` 为标准前缀），脚本未剥离前缀所致；② contact.html 197 字被标「薄内容」—— 实为含邮箱+3 条 FAQ 的真实内容，联系页本就偏薄，非问题。
- 注：`prerender.mjs` 第 465 行 `alternates` 未读取为既有 HINT，非本次引入，未改动。
