# 2026-07-14

## 推进 AdSense「低价值内容」整改 — P2 技术可抓取性

- 项目：qingzao-homepage（React19 + Vite6 SPA，Vercel 部署）
- 背景：`docs/Ads/googleAds政策/低价值内容改进计划0714.md` 诊断 AdSense 被拒根因为「低价值内容」(规模化内容 + 正文靠 JS 运行时渲染)。P0(删离题文章) 此前已完成，当前 13 篇博客文章与「保留清单」一致。
- 本次完成 P2 第 7 项（预渲染博客正文为静态 HTML）：
  - 新增 `src/data/posts/markdownToHtml.js`（纯 JS，无 React/JSX 依赖），供客户端组件与 Node 构建脚本共用；顺带修复了原 `BlogPostPage` 缺失的引用块(`>`)渲染，改为 `<blockquote>`。
  - 重构 `src/pages/BlogPostPage.jsx`：移除内联 `renderInline`/`renderContent`，改用 `markdownToHtml` + `dangerouslySetInnerHTML`。
  - 重写 `scripts/prerender.mjs`：`vite build` 后，为每篇博客生成 `dist/blog/<slug>/index.html`，首屏即含全文 + 正确 `<title>`/`description`/`canonical`/JSON-LD。关键修复：模板只读一次（曾因 `/` 路由覆盖 `dist/index.html` 导致后续路由 meta 重复/错误 canonical）。
  - 中英文文章共用同一 slug，静态文件只预渲染默认语言(中文)，英文由 SPA 切换。
- 验证：`npm run build:full` 成功生成 22 个路由(13 篇博客)；Chrome DevTools 实测文章页正常渲染、无报错；已确认每文件仅一个 canonical。
- P2 第 8 项（提交 sitemap 到 GSC）为人工操作：sitemap.xml 已由 `generate-sitemap.mjs` 生成(20 URL，无重复)，需在 GSC 后台提交。
- 相关命令：`npm run build:full`（= generate-sitemap + vite build + prerender）；`npm run preview`(端口4173) 用于本地预览 dist。
