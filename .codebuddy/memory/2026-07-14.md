# 2026-07-14

## AdSense「低价值内容」整改 — P0 执行完成
背景：网站被 Google AdSense 判「低价值内容/规模化内容」。诊断文档见 `docs/Ads/googleAds政策/低价值内容改进计划0714.md`。

P0 已执行：
1. 删除 9 篇离题文章（电动车、健康饮食、正念、Python、React、Web设计、创业营销、时间管理、SEO），从 `src/data/posts/posts-zh.json` 与 `posts-en.json` 移除条目，并删除对应 18 个 `-zh.md`/`-en.md`（在 `src/data/posts/articles/`）。博客从 22 篇聚焦到 13 篇（装修线 8 + 图片线 4 + 工具线 1）。
2. 顺带清理无用文件：`articles/article-meta.json`（未被引用的批量生成残留）、`articles/smart-home-system.md`（孤立重复，loader 永不用）、`posts-zh.json.backup`。
3. 删除根目录冗余批量生成脚本：`add-posts.mjs`、`generate-*.mjs`（8 个）、`simple-generate.mjs`、`verify-posts.mjs`、`fix_json_format.py`、`fix-json.js`。保留 `scripts/generate-sitemap.mjs`、`scripts/prerender.mjs`（build:full 需要）。
4. 重新生成 `public/sitemap.xml`（`node scripts/generate-sitemap.mjs`，20 URL = 7 静态 + 13 文章），`npm run build` 通过（122 模块）。

## 关键项目知识
- 博客数据源：`src/data/posts/posts-zh.json` / `posts-en.json`（被 BlogListPage/BlogPostPage/ContentHighlights 引用）。正文用 `postContentLoader.js` 的 `import.meta.glob('./articles/*.{md,txt}')` 运行时按 `slug-{zh|en}.md` 加载。
- sitemap 由 `scripts/generate-sitemap.mjs` 读取 posts-zh.json 自动生成，改文章后需重跑 `npm run sitemap`。
- 后续待办：P1（深化保留文章、加署名/编辑方针页、文章内链）、P2（博客正文预渲染静态 HTML，已有 scripts/prerender.mjs）、P3（Search Console 自查后重新提交审核）。
